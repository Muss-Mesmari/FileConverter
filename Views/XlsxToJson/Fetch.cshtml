@*@model IEnumerable<ExcelSheet>*@
@model FileConverter.ViewModels.DocumentFileViewModel

@{
    ViewData["Title"] = "Fetch";
}

<h1>Fetch</h1>


<div>
    @using (Html.BeginForm("Fetch", "XlsxToJson", FormMethod.Post))
    {
        <input type="submit" value="Get data" />
    }
    @if (Model.ExcelSheet.Column != null)
    {
        <hr />
        <table cellpadding="0" cellspacing="0" border="1">

            @foreach (var item in Model.ExcelSheet.Column)
            {
                <tr>
                    <th>Row</th>
                </tr>
                <tr>
                    <td>@item</td>
                </tr>
            }
        </table>
    }
</div>
